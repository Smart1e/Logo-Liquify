
Using the music app as an example.


Has the following data points in the .plist (Contents/info.plist)
	Icon file
	Icon Name

That will link directly to a .icns, and has variants in the assets.car folder